# Changelog

## [3.1.0] - 2026-08-19

### 新增

- 字段映射高级模式：左侧字段名改为可编辑输入框（自绘候选弹层列出 14 项标准字段，也可自由输入自定义字段名如「权利人」）；手输恰好命中标准字段名（如「图斑面积」）时该行自动恢复占位/平方米/公顷等专属选项
- 锁定能力与字段名解耦：「坐标点个数」「图形属性」改名后仍自动统计点数/固定「面」（锁定判定改按映射源，后端点数重算改为哨兵传递，改名后点数列不再恒为 0）
- 用户字段方案增删改查：预设下拉动态追加自定义方案——「存为方案」输入名称保存（同名确认覆盖即更新）、下拉选中即载入整套字段清单、选中自定义方案时可删除；方案存于本机，改动任何行后自动回到「自定义」态
- 设置面板：主题/色系、显示比例、三区字号缩放（标题栏+弹窗 / 折叠卡内容 / 预览文本）统一收纳进一个「设置」弹窗，标题栏主题按钮简化为一键明暗切换
- 赞赏与关于弹窗合并：点「关于」一个弹窗依次展示赞赏码、交流群、功能介绍与更新日志（原独立赞赏弹窗移除）

## [3.0.0] - 2026-08-19

### UI 全面重构（方案A 折叠收纳式，原型见 docs/mockups/）

- 布局：三栏比例 1:1:1.3（默认窗口增至 1100×720），左栏 = ①导入数据 + ②字段映射，中栏 = ③输出与选项（含输出目录）+ ④动态投影 + ⑤自定义表头，TXT→面 同步手风琴化（①导入 TXT + ②输出设置）
- 手风琴分组卡：编号 + 名称 + 状态标签，点击标题折叠/展开；各栏分组拉伸三栏底边齐平，内容超高卡内滚动
- 主题系统：主题按钮改为下拉面板（浅色/暗色 + 8 个色系：经典黑白（默认）/ 测绘黄铜 / 墨绿 / 海蓝 / 青蓝 / 绛紫 / 珊瑚橙 / 玫红），色系为全套配色（背景/表面/强调/预览联动），浅/暗 × 色系独立组合共 16 种，选择持久化
- 预览区：浅色模式下字体统一黑色（暗色模式保持浅色）
- 字段映射：高级模式开关移至分组标题行；「补充耕地模式」改为预设下拉（8 字段标准 / 补充耕地模式 / 自定义），手动修改任何行后自动变为「自定义」
- 显示比例预设更新为：三栏等宽 / 默认(1:1:1.3) / 宽预览 / 宽输入
- 窗口长宽+位置记忆：拖动调整窗口尺寸或移动位置后自动保存（最大化不记、恢复时做屏幕内 clamp 防窗口跑出屏幕），下次打开恢复上次大小和位置；所有设置内容（预设方案/字段映射/表头/主题/色系/比例）本就持久化，与窗口状态一起完整还原上一次工作状态

## [2.2.0] - 2026-08-18

### 新增

- 字段映射「高级模式」：开关切换（默认关，简单模式输出字节级不变）。开启后字段列表化——14 项固定字段清单下拉、拖拽排序、增删行，交互与「属性描述」区一致
- 「补充耕地模式」预设开关：一键载入 12 字段模板（坐标点个数/图斑面积/图斑编号/地块名称/补充耕地实施年份/耕地坡度级别/图形属性/图幅号/地块用途/备注/地类/耕地质量等级）；手动改行后呈「自定义」灰态，防误触覆盖
- 特殊字段锁定：坐标点个数（自动统计点数）、图形属性（固定「面」）参与排序但不可删不可改映射源
- 高级格式输出：元数据行按自定义列序输出（不输出字段名列表行，接收系统按约定列序解析）；导入外部自带 `【字段1,...,@】` 列表行的 TXT 仍按名正确解析
- 首次开启高级模式自动继承简单模式 6 个下拉已选的源字段映射
- 反向导入全面支持新格式：字段名列表行/「地块坐标要求」说明块正确跳过（此前列表行会被误判为元数据行导致数据错乱）、12 个元数据字段按名解析全保留
- TXT→SHP 动态字段：非标准 8 字段格式输出 FIELD1~FIELDn 全字段（数值列自动推断 N 类型），标准格式维持 DKMC/DKBH/MJ/DKYT/TFH/DLBM

### 修复

- TXT→SHP 输出 DKBH 恒为空的问题：现填元数据行第 3 列（地块/图斑编号）
- 预设恢复时字段映射 6 个下拉不生效的问题（f 键名与 DOM id 不匹配导致恢复 no-op）
- 元数据行解析：末尾连续空字段不再被整段剥掉（新格式末列空值场景列数对不上会误回退旧格式）

## [2.1.0] - 2026-08-13

### 修复

- 修复 ArcGIS Pro 启用 Z+M 的 GDB 无法导入的问题（shapefile 风格类型码 15=PolygonZM 等未正确映射回 2D）

## [2.0.0] - 2026-08-04

### 新增

- 动态投影弹窗重构：目标形式下拉 + 带号↔中央经线双向联动
- 同分带换带：支持 3°带/6°带内不同带号互转（如 38 带→39 带）
- 预览与导出一致：修复导出用原始坐标的 bug
- WGS84 投影坐标系支持
- 投影数据推荐：显示经纬度范围 + 推荐带号/中央经线
- 预览加载动画 + 带号限位（3°带 24-45 / 6°带 13-23）
- 感谢 @alan 的 PR 贡献

## [1.5.0] - 2026-06-30

### 新增

- 界址点 J 编号改为单个地块内跨环（外环/洞/多部件）连续递增，每个地块仍从 J1 起；闭合点（首末点重合）回写本环首点序号
- 「首末点重合」新增「闭合点编号」下拉：可选「回到环首点」（默认）或「续编」（闭合点占下一个连续序号）；未勾首末点重合时下拉置灰；选项随预设保存/恢复

## [1.4.0] - 2026-06-29

### 新增

- 「属性描述」段高度自定义：原 7 个固定项（坐标系/几度分带/投影类型/计量单位/带号/精度/转换参数）改为可增删改键名、拖拽调顺序的属性行列表，可在任意位置插入自定义行（如「格式版本号」「数据产生单位」「数据产生日期」）
- 属性行拖拽排序：按住行首 ⠿ 手柄上下拖动即可调整顺序
- 「精度」「几度分带」字段恢复下拉选择（固定候选值）
- 属性行过多时列表区限高滚动，按钮与标签页固定不被挤掉
- 预设方案保存/加载完整保留自定义属性行与顺序；旧版本预设加载时自动迁移

### 改进

- 空「键=值」行不输出（非强制）；「精度」行被删或改名时坐标小数位自动兜底 0.001

## [1.3.0] - 2026-06-26

### 新增

- 应用内自动更新：启动时自动检查新版本，标题栏常驻三态按钮（默认刷新图标 / 有新版本绿色箭头 / 已跳过灰色箭头），点击即可在应用内下载安装并自动重启，无需再去网盘手动下载
- 更新提醒弹窗支持「跳过此版本」、24 小时节流（避免频繁请求）、下载失败兜底跳转百度云手动下载
- 国内用户检查更新走 jsDelivr + ghproxy 镜像加速

### 修复

- 导入含中文 DBF 字段名（如"村民姓/行政村/备注"）的 SHP 文件时软件闪退（dbase crate 无法解析非 ASCII 字段名，改为 catch_unwind 防护 + 手动二进制解析回退）
- 导入 GBK 编码的 SHP（DBF 无 .cpg 文件）/ TXT 时无反应或乱码（现自动探测 BOM / UTF-8 / GBK 编码）
- DBF 字段名/值编码探测：优先读 .cpg 文件，无 .cpg 时采样 Character 字段值字节判定 GBK

## [1.0.0] - 2026-06-24

## [1.0.0] - 2026-06-24

首次公开发布。

### 新增

- 面 → TXT 双向转换（导入 SHP / GDB 面要素，输出标准界址点 TXT）
- TXT → 面转换（解析 TXT，生成 SHP 矢量面数据）
- 三种输出模式：一对一 / 按地块拆分 / 全合并
- 支持 2000 国家大地坐标系、1980 西安、1954 北京、WGS84
- 高斯克吕格投影 3°/6° 分带，带号自动提取
- 统一字段映射面板，字段自动匹配
- 自动面积计算
- 浅色 / 暗色主题切换
- 自定义无边框窗口（880×600）
